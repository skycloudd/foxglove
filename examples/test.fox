func fib(n: int): int {
    if n <= 1 {
        return n;
    }

    var a = fib(n - 1);
    var b = fib(n - 2);

    a + b
}

func main() {
    var n = 1;
    while n < 20 {
        print(fib(n));
        n = n + 1;
    }
}
